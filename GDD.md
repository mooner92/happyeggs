# EGG FLIP (가제) — GDD & Claude Code 개발 프롬프트 v0.1

> 이 문서는 이 프로젝트의 단일 진실 소스(SSOT)다. 구현 중 이 문서와 충돌하거나 문서에 없는 판단이 필요하면 **임의로 결정하지 말고** docs/DECISIONS.md에 [DECISION] 항목으로 추가한 뒤 나에게 물어라.

---

## 0. 너의 역할과 작업 규약

- 너는 이 프로젝트의 **리드 게임 프로그래머**다. 나는 기획/디렉터이고 게임 개발 방법론에 익숙하지 않다. 전문 용어는 써도 되지만, 선택지가 갈리는 지점은 트레이드오프를 1~2줄로 요약해서 물어봐라.
- **마일스톤(§13) 순서대로만 진행한다.** 각 마일스톤 시작 시: 파일 단위 구현 계획 제시 → 내 승인 → 구현 → 실행/테스트 방법 보고 → **정지하고 리뷰 대기**. 마일스톤을 건너뛰거나 병합하지 마라.
- 커밋은 작게, conventional commits(feat:, fix:, refactor:). 브랜치는 `feat/m0-skeleton` 형식.
- 초기 셋업 시 `CLAUDE.md` 생성: 스택/실행법/규약 요약 + "상세 스펙은 GDD.md 참조" 명시.
- 유지 문서: `ARCHITECTURE.md`(시스템 구조), `docs/DECISIONS.md`(미결/결정 로그, ADR-lite).
- 밸런스 수치(시간, 계수, 판정 윈도우, 감점량)는 **전부 `src/data/balance.ts` 한 파일**에 상수로. 매직넘버 인라인 금지.
- 유닛테스트는 순수 로직(채점, 익힘 모델, 뒤집기 판정, 이벤트 스케줄러)에만 작성. 렌더링 테스트 금지.
- 코드/식별자는 영어, 주석과 문서는 한국어 허용.

## 1. 한 줄 컨셉

1인칭 계란후라이 아케이드. 손님 주문대로 계란을 깨서 굽고, 낚시 캐스팅식 파워 게이지로 팬을 뒤집고, 사방에서 난입하는 방해꾼을 몇 초 안에 **올바른 입력**으로 처리하면서, 최대한 완벽한 원에 가까운 후라이를 만든다. 점수는 원형도 기반 소수점 3자리(100.000점은 사실상 불가능).

레퍼런스 톤: **Bacon – the game (kamibox)** — 미니멀하고 부드러운 손그림 느낌의 2D, 유머 있는 연출, 오브젝트 소수 정예.

## 2. 타깃 / 플랫폼 / 성능 예산

- 1차 타깃: **모바일 웹(세로 9:16)** + 데스크톱 웹. 2차: Capacitor(iOS/Android 앱), Electron + steamworks.js(Steam 무료 배포 + 스킨 MTX).
- 성능: 미드레인지 안드로이드 크롬에서 60fps. 초기 로드 번들 < 3MB(에셋 lazy load). update 루프 내 per-frame 객체 할당 금지. 적/파티클/말풍선은 오브젝트 풀링. 텍스처는 아틀라스.
- 입력은 `pointerdown` 기준(click의 지연 금지). 마우스=터치 동일 매핑.
- 가로 화면(데스크톱): 세로 액션 칼럼을 중앙 고정하고 좌우로 주방 배경 아트를 확장(단순 레터박스 금지). **[DECISION-05]**

## 3. 기술 스택 (고정 — 변경 제안 금지)

- **Phaser 3 최신 안정판 + TypeScript(strict) + Vite**
- 상태 관리: 씬 로컬 상태 + 자체 경량 EventBus. 외부 상태관리 라이브러리 금지.
- 계란 블롭 렌더: `Phaser.GameObjects.Graphics` 폴리곤 + 스무딩. 물리엔진 미사용(자체 간이 시뮬).
- 저장: localStorage 래퍼(schema version 필드 포함) — 진행도/스킨.
- 공유: 오프스크린 Canvas 합성 → PNG → Web Share API(미지원 시 다운로드 + 클립보드).
- 테스트: Vitest. 린트: eslint + prettier 기본 설정.
- 배포: 정적 빌드. vite `base` 옵션 유연하게(Vercel / GitHub Pages / itch.io 겨냥).

## 4. 화면 구성 / 입력 사전

```
┌────────────────────┐
│ 원경 (건물 → 저격수 등장)   │
│ 손님 대기열 (말풍선 주문)    │
│      [주방 카운터]          │
│  아이템   아이템   아이템    │  ← 스테이지별 배치 상이
│        🍳 팬 (중앙)         │
│ 왼손(팬 손잡이)  오른손(뒤집개)│  ← 1인칭 양손
│ HUD: 계란재고 / 점수 / 손님수 │
└────────────────────┘
```

입력 사전 (전 플랫폼 공통):

| 제스처 | 용도 |
|---|---|
| 탭 | 계란 깨기, 아이템 사용, 파리 처치, 스토브 재점화 |
| 더블탭 (250ms 내 2회) | 고양이 소환 (강도 퇴치) |
| 홀드 → 릴리즈 | 팬 뒤집기 파워 게이지 |
| 드래그 | 거미줄 절단 |
| 스와이프 (손님 방향) | 완성 후라이 서빙 **[DECISION-03]** |

## 5. 코어 루프 (스테이지 내)

1. 손님 줄에서 맨 앞 손님이 주문 — 말풍선에 계란 아이콘 × N.
2. 플레이어가 계란을 **N번 탭** → 팬 위에 깨짐(블롭 생성). 깨는 위치·간격이 최종 모양에 영향.
3. 익힘 진행(열원별 속도, §6.2). 흰자가 퍼지는 시뮬레이션.
4. **홀드-릴리즈**로 뒤집기(§6.3) → 짧은 2차 익힘 → 손님 방향 스와이프로 서빙.
5. 서빙 즉시 원형도 채점(§6.4) 팝업, 소수점 3자리.
6. 방해꾼 이벤트(§8)가 2~5 사이에 랜덤 삽입되어 멀티태스킹 압박.
7. 스테이지 종료 조건: **성공** = 대기열 소진 / **실패** = 스프링클러 발동, 계란 재고 < 남은 주문량, 즉사형 이벤트(재채기).

전체 템포는 **스피디하게**: 익힘은 짧고(스테이지 초반 기준 8~15초/판), 이벤트 전조는 짧고 명확하게.

## 6. 계란 시스템 — 핵심, 유닛테스트 필수

### 6.1 표현

- 계란 1개 = 중심 기준 **방사형 정점 40~64개 폴리곤 블롭**. 시간에 따라 정점이 바깥으로 퍼짐(정점별 Perlin 노이즈 + 이웃 정점 스무딩) → 자연스러운 비정형 원.
- 깨는 위치가 팬 중앙에서 멀수록, 다른 계란과 가까울수록 초기 형태 불리(겹치면 흰자 융합 — 융합된 덩어리는 개별 채점 불가, 평균 폭락).
- **노른자**: 중심 근처 원. 충격(과한 뒤집기)이나 강도 이벤트로 터질 수 있음 → 터지면 해당 계란 **-15.000점** 고정 감점.
- 렌더: RAW(반투명) → 익을수록 흰자 불투명 + 가장자리 갈변 → BURNT(뒷면 검정).

### 6.2 익힘 상태머신 + 열원

`RAW → SET → PERFECT_WINDOW → OVERDONE → BURNT → SMOKE`

| 열원 | 속도 계수 | 특성 |
|---|---|---|
| 가스 스토브 | 1.0 | 기본. 야간 스테이지 "불 끄기" 이벤트 대상 |
| 인덕션 | 0.8 | 균일하고 느긋함 |
| 화롯불 | 1.3 | 가장자리부터 탐 |
| 캠프파이어 | 1.5 ± 0.3 랜덤 요동 | 불규칙 — 게이지 주시 필요 |
| 용암 분출구 | 2.5 | PERFECT_WINDOW 극단적으로 짧음 |

- SMOKE 진입 후 3초 방치 → 검은 연기 → **스프링클러 발동 연출 → 스테이지 즉시 실패**.

### 6.3 뒤집기 판정

- 게이지: 홀드하면 0→1→0 **왕복형** 게이지(낚시 캐스팅식). 릴리즈 시점 값 = p.
- 판정 = (릴리즈 순간의 익힘 상태) × (p가 스테이지별 스윗스팟 범위 안인지):

| 상태 | 결과 |
|---|---|
| **RAW에서 뒤집기** | 계란이 발사체가 되어 **앞 손님 피격** → 손님이 가게 물건(아이템) 1개 훔쳐 도주. 훔친 게 대응 아이템(예: 펜싱칼)이면 해당 이벤트 **방어 불가 상태** 돌입 |
| SET~PERFECT + p 스윗스팟 | 클린 착지(변형 최소) — 만점 루트 |
| SET~PERFECT + p 부족 | 반접힘 — 폴리곤 대변형, 점수 폭락 |
| SET~PERFECT + p 과다 | 팬 밖 이탈 — 계란 로스트(재고에서 이미 소모됨, 다시 깨야 함) |
| **OVERDONE/BURNT에서 뒤집기** | 까만 뒷면 공개 → 현재 손님이 분노, **재고에서 계란 N개(자기 주문 수) 훔쳐 도주** → 주문 실패 |
| 안 뒤집고 방치 | SMOKE → 스프링클러 → 스테이지 종료 |

### 6.4 원형도 채점

- 등주부등식 지수: **Q = 4πA / P²** (완벽한 원 = 1.0)
- 구멍(총알): A_eff = A − ΣA_hole, P_eff = P + ΣP_hole → 자연 감점.
- 반토막(거미): 남은 폴리곤으로 그대로 계산 → 점수 폭락.
- `score = floor(Q_eff × 100 × 1000) / 1000` → **"97.412"** 형식 표기. 노이즈 시뮬 특성상 100.000 도달 불가(의도).
- 스테이지 점수 = 서빙 성공한 계란들의 평균(소수점 3자리). 주문 실패 손님의 계란은 미집계, 대신 별점 감점(§10).
- **이 함수 전체 유닛테스트 필수** (완벽 원, 반토막, 구멍 1~2개, 융합 케이스).

### 6.5 계란 재고

- 스테이지 시작 시 `eggStock = 주문 총합 + 여유분(스테이지 데이터)`.
- 깨기/로스트/도난 모두 재고 차감. **남은 재고 < 남은 주문 총량 판정 시 즉시 실패** ("마지막 손님 주문량 때문에 완료 불가" 상황 구현).

## 7. 손님 / 주문

- 대기열은 화면에 3~4명 보이게. 맨 앞 손님만 활성 주문.
- v1에서는 손님 인내심 타이머 없음 — 시간 압박은 익힘 타이머와 이벤트에서 나온다.
- 특수 손님(재채기꾼, 머리카락 트롤)은 일반 손님 스킨으로 줄에 섞여 들어와 이벤트 트리거가 된다(§8).

## 8. 방해꾼 이벤트 시스템 — 데이터 주도 설계

**새 적 추가가 "JSON 항목 + 핸들러 1개 등록"만으로 가능해야 한다.** 공통 스키마:

```jsonc
{
  "id": "ninja_spider",
  "stageUnlock": 2,
  "telegraphMs": 900,          // 전조 연출 시간
  "responseWindowMs": 1800,    // 대응 입력 허용 시간
  "input": "drag_cut",         // 입력 핸들러 키
  "cooldownMs": [8000, 15000],
  "maxConcurrent": 1,
  "onSuccess": ["fx_web_flutter", "sfx_stomp_offscreen"],
  "onFail": ["egg_bisect", "actor_escape"]
}
```

이벤트 스케줄러: 조리 중에만 발생, 스테이지별 `eventBudget` 소진까지 랜덤 간격. 동시 이벤트는 스테이지 후반부터 허용.

### 8.1 적 스펙 (전부 구현 대상)

**① 닌자 거미** — 천장에서 칼 들고 거미줄 타고 하강.
- 대응: 거미줄을 **드래그로 절단** → 왼손이 팬을 잠깐 옆으로 치우는 연출 + 거미 화면 아래로 낙하(화면 밖) + **밟아 죽이는 소리(SFX만, 비주얼 없음)** → 잘린 거미줄이 **라운드 끝까지 바람에 휘날림**(트로피 연출).
- 실패: 후라이를 순식간에 **반토막**(폴리곤 절단) 내고 사라짐.

**② 뒷문 강도** — 가게 뒷문으로 슬금슬금 진입.
- 대응: **빠른 더블탭** → 고양이가 튀어나와 쫓아냄(야옹 + 후다닥 SFX).
- 실패: 숟가락으로 **노른자를 확 떠서** 훔쳐 도주(해당 계란 노른자 파손 상태).

**③ 재채기 손님 (트롤)** — 대기열 앞 손님이 재채기 빌드업(코 벌렁, "에... 에취" 오디오 빌드업). 애초에 살 생각 없이 트롤하러 온 손님.
- 대응(기본안): **팬 뚜껑 아이템 탭** → 뚜껑으로 팬 가림 → 침 차단 → 트롤 머쓱하게 퇴장. **[DECISION-01]** (대안: 티슈 박스 탭해서 건네기)
- 실패: 침 튀김 → 후라이 오염 + 손님 도주 → **즉시 게임 오버**.

**④ 머리카락 손님 (트롤)** — 머리카락 하나 뽑아 팬 위로 던지려 함. 머리카락이 슬로모션으로 낙하.
- 대응(기본안): **토치 아이템 탭** → 공중에서 소각, '치익⚡' SFX + 작은 재가 팬 옆으로 떨어짐 + 트롤 표정 굳음. **[DECISION-02]** (대안 A: 낙하 중 스와이프로 뒤집개 휘둘러 쳐내기 / 대안 B: 핀셋으로 낙하 중 캐치)
- 실패: 머리카락 팬 착지 → 해당 계란 **-10.000점** + 트롤 낄낄대며 도주.

**⑤ 저격수** — 원경 건물에서 레이저 조준선이 팬을 향함.
- **함정**: 조준 중 저격수(또는 레이저)를 직접 탭하면 저격수 **+1명 증원(최대 3명)** — 패닉 탭을 유도하는 트랩. **[DECISION-04]**
- 발사: 총구 화염 표시 → **1초 안에 주방의 펜싱칼 탭** → 패링('깡' + 리코셰 SFX) → 총알 반사로 저격수 명중, 건물에서 추락.
- 실패: 후라이에 **구멍**(채점에 반영). **프라이팬 자체는 방탄** — 팬에 맞으면 탄흔 스파크만(배틀그라운드 오마주).

**⑥ 파리** — 랜덤 궤적으로 비행.
- 비행 중 탭 → **잡히지 않음**. 화면 앞으로 다가와 **티배깅 3회** 후 도주(그동안 조리는 계속 = 어그로용).
- 파리가 착지 후 **바지 내리고 똥 싸려는 모션**(1초 전조) → 이때 탭 → **별이 되어 반짝 사라짐**(★ 이펙트 + 처치 성공).
- 방치: 똥 완료 → 해당 계란 **-20.000점** + 도주.

**⑦ 야간 스테이지 전용** — 화면 전체 **열화상 렌즈** 필터(§8.2).
- 불 끄는 적: 슬쩍 다가와 가스불 끔 → 익힘 정지 → **스토브 탭으로 재점화**.
- 바리에이션: 불 끄고 **불 모양 스티커**를 붙여놓는 적 → 일반 시야면 속지만, **열화상에서는 스티커가 차갑게(파랗게) 보여서 구분 가능** — 열화상 컨셉과 트릭의 시너지 포인트.

**⑧ 지역 확장 (데이터로만 추가)** — 사막/남극 등 배경 테마별 동물형 적(펭귄, 악어 등). v1 구현 불필요, **스키마가 수용 가능함을 증명**만 하면 됨(더미 JSON 1개).

## 9. 주방 아이템 & 어포던스 미스리드

- 아이템(뚜껑, 펜싱칼, 방패, 토치, 핀셋 등)은 스테이지 데이터로 위치 정의(벽걸이 / 스토브 옆 / 선반 등 다양하게).
- **의도적 미스리드가 핵심 재미**: 예) 방패+칼이 나란히 있을 때 저격 이벤트의 직관적 정답은 방패지만, 실제 정답은 **칼 패링**. 방패 탭 시 → 방패는 장식이라 손잡이가 빠지는 개그 연출 + 총알은 그대로 → 구멍. "죽어보고 배우는" 디자인.
- 미스리드 실패의 페널티는 **점수 손해 수준**으로 제한(즉시 게임오버는 재채기만). 러닝커브 보호. **[DECISION-06: 페널티 강도]**
- 오답 아이템(decoy)도 스키마에 포함: `{"id": "shield_decoy", "correctFor": null, "failGag": "handle_falls_off"}`

## 10. 스테이지 데이터 & 진행

```jsonc
{
  "id": "stage_04",
  "background": "kitchen_day",
  "heatSource": "campfire",
  "customers": 6,
  "orderRange": [1, 3],
  "panCapacity": 2,              // 팬 동시 계란 수 (초반 1, 후반 최대 3) [DECISION-07]
  "eggStock": 14,
  "enemyPool": ["ninja_spider", "back_robber", "fly"],
  "eventBudget": 5,
  "items": [
    {"id": "lid", "pos": "wall_left"},
    {"id": "fencing_sword", "pos": "stove_side"},
    {"id": "shield_decoy", "pos": "stove_side"}
  ],
  "starThresholds": [80.0, 90.0, 96.0]   // 평균 점수 기준 별 1~3
}
```

- 난이도 곡선: 스테이지 1~3은 튜토리얼성(적 1종씩 소개, 열원 가스 고정).
- 주문 실패 손님(계란 도난 등)이 있어도 스테이지는 계속 진행 — 별점만 감점. 즉시 실패는 §5의 실패 조건만.

## 11. 결과 화면 & 공유

- 클리어 시: 만든 후라이들이 접시 위에 **쫙 배열**되는 연출 → 각각의 원형도 점수 카운트업 → 평균/최고점 + 별점.
- 결과 화면을 오프스크린 Canvas로 합성 → PNG. 모바일: **Web Share API**(IG 등 공유 시트). 데스크톱: 다운로드 + 클립보드 복사.
- 파일명 규칙: `eggflip_stage04_92.317.png`

## 12. 스킨 / BM 스캐폴드

- 계란 스킨 = 메인 BM. **순수 코스메틱** — 껍데기 색/무늬, 노른자 색, 깨질 때 이펙트 차등. 게임플레이 영향 0.
- v1 범위: 스킨 데이터 스키마 + 상점 UI + 소유/장착 localStorage 저장까지. **결제는 스텁**(버튼만).

```jsonc
{"id": "egg_galaxy", "shell": "#2b1d4e", "pattern": "stars", "yolk": "#ffd34d", "crackFx": "sparkle", "price": 0}
```

## 13. 마일스톤 — 이 순서로만, 각 마일스톤 종료 시 정지

**M0 — 뼈대**: Vite+Phaser+TS 셋업, 씬 구조(Boot/Preload/Game/Result), 세로 레이아웃 + 스케일 매니저, 양손+팬 렌더(도형), 탭으로 계란 깨기 → 블롭 생성/퍼짐, 익힘 FSM(색 변화), 디버그 HUD(상태/타이머/fps).
→ 인수 조건: `npm run dev`로 폰 브라우저에서 계란 깨고 타는 것까지 눈으로 확인 가능. 익힘 모델 Vitest 통과.

**M1 — 코어 루프**: 왕복 파워 게이지 + 뒤집기 판정 전부(발사/반접힘/이탈/클린), 팬 틸트 + 계란 포물선 + 착지 스쿼시 애니, 원형도 채점 + 유닛테스트, 스와이프 서빙, 손님 큐/말풍선/재고 HUD, 하드코딩 스테이지 1개 클리어 가능.

**M2 — 이벤트 프레임워크**: 스케줄러(eventBudget/cooldown/조리 중 발생), 공통 telegraph→window→resolve 파이프라인, **거미 + 강도** 2종 완전 구현(성공/실패 연출 포함).

**M3 — 스테이지 시스템**: JSON 스테이지 로더, 실패 조건 3종(스프링클러/재고 부족/즉사), 결과 화면(후라이 배열 + 카운트업 + 별점), PNG 합성/저장/Web Share.

**M4 — 적 확장 + 아이템**: 저격수(증식 트랩 + 패링), 파리(티배깅/별 처치), 재채기, 머리카락, 아이템 배치 시스템 + 미스리드(방패 decoy), **아이템 도난 → 방어 불가 상태** 연쇄.

**M5 — 야간 + 스킨**: 열화상 셰이더(팔레트 스왑: 뜨거운 것만 밝음), 불 끄기 적 + 가짜불 스티커 변형, 스킨 스키마/상점 UI/장착 저장.

**M6 — 폴리시**: 이징/스쿼시&스트레치/파티클/화면 흔들림, SFX 스텁 훅 전체 연결(§14), 성능 패스(아틀라스/풀링 점검, 폰 실측 60fps), balance.ts 1차 튜닝 시트.

**M7 (별도 승인 후 착수)**: Capacitor 앱 래핑, Electron + steamworks.js 스팀 빌드 — 지금은 문서화만.

## 14. 아트 / 사운드 파이프라인 (현 단계 규약)

- 지금은 **전부 placeholder programmer art**(Graphics 도형 + 제한 팔레트 5색). 단, 최종 아트 교체를 전제로 **에셋 키 매니페스트**(`src/data/assets.ts`)를 처음부터 유지 — 나중에 키만 스프라이트로 스왑.
- 애니메이션: 트윈 기반 절차 애니 우선(손, 팬 틸트, 적 등장/퇴장). 스프라이트시트 로딩 훅만 준비.
- SFX 리스트(지금은 무음 스텁, 훅만): 계란 깨짐, 지글지글 루프(익힘도 따라 피치 상승), 밟는 소리(오프스크린), 패링 '깡'+리코셰, '치익'(머리카락 소각), 재채기 빌드업, 고양이, 티배깅 뿅뿅, 별 처치 반짝, 연기 경보+스프링클러, 스테이지 클리어 팡파레.

## 15. 톤앤매너

- 유머 유지: 티배깅, 배그 방탄팬 오마주, 별똥별 처치. 폭력은 **만화적 + 오프스크린**(거미는 소리로만 처리) — IG 공유/스팀 전연령 지향.
- UI 텍스트 최소화, 아이콘/연출로 전달(Bacon 스타일).

## 16. [DECISION] 미결 목록 — 구현 착수 전 나에게 확인할 것

| # | 항목 | 기본안 |
|---|---|---|
| 01 | 재채기 대응 | 팬 뚜껑 탭 (대안: 티슈 건네기) |
| 02 | 머리카락 대응 | 토치 공중 소각 (대안: 뒤집개 스와이프 쳐내기 / 핀셋 캐치) |
| 03 | 서빙 방식 | 손님 방향 스와이프 (대안: 2차 익힘 완료 시 자동) |
| 04 | 저격수 증식 트리거 | 저격수/레이저 직접 탭 시 +1 (화면 전체 탭은 조리 불가라 제외) |
| 05 | 가로 화면 정책 | 중앙 액션 칼럼 + 좌우 배경 확장 |
| 06 | 미스리드 실패 페널티 | 점수 손해 (즉사 아님) |
| 07 | 팬 동시 계란 수 | 스테이지별 1~3 |

---

**시작 지시**: 위 §0 규약대로, 먼저 M0의 파일 단위 계획을 제시하라. 계획에는 디렉터리 구조 초안(scenes/ systems/ data/ ui/)과 익힘 FSM의 상태 전이표를 포함할 것.
